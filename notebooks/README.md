# Notebooks

Optional exploration notebooks can live here. The supported reproduction path is:

```
uv run python scripts/reproduce_all.py
```

Do not treat notebooks as the source of published numbers; those come from `phaseforge/` and `results/`.
